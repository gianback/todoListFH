//agrupa todas las clases para exportarlas para todos mis archivos y solo importar este archivo con todas las clases importadas
import { Todo } from "./todo.class";
import { TodoList } from "./todo-list.class";

export { Todo, TodoList };
